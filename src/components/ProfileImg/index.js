import styled from 'styled-components';

const ProfileImg = styled.img`
    border-radius: 50%;
    width: 100%;
    max-width: 20rem;
    height: auto;
    padding: 2rem 1rem;
`;

export default ProfileImg;